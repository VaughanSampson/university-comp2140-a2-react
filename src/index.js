import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';    

// Creates root.
const root = ReactDOM.createRoot(document.getElementById('root'));

// Renders everything on root.
root.render(
  <React.StrictMode>
    <App /> 
  </React.StrictMode>
);
 
